# Railway deployment configuration for backend
# This file is read by Railway to configure the deployment

[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/"
